{
	"name": "frontend",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "astro dev --host 0.0.0.0",
		"start": "astro dev --host 0.0.0.0",
		"build": "astro build",
		"preview": "astro preview",
		"privacy": "astro telemetry disable"
	},
	"dependencies": {
		"astro": "7.1.4"
	},
	"devDependencies": {},
	"peerDependencies": {}
}
